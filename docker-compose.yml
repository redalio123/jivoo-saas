services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: jivoo
    ports:
      - "5432:5432"
    volumes:
      - jivoo_pg:/var/lib/postgresql/data
volumes:
  jivoo_pg:
